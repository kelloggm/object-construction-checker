// A test that checks that an empty @AlwaysCall annotation in a stub on a subclass overrides
// a non-empty one on a superclass (that is being inherited).

class InheritanceStream {
    void test() {

    }
}